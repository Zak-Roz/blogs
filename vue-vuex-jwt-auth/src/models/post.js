export default class Post {
  constructor(title, description, content, username, images) {
    this.title = title;
    this.description = description;
    this.content = content;
    this.username = username;
    this.images = images;
  }
}
